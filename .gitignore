# Virtual environment — thousands of files, never push this
venv/

# Your secret credentials — NEVER push this to GitHub
.env

# The dataset — too large for GitHub, setup.py handles downloading it
data/

# Saved model files — these get regenerated by running the project
models/

# Python cache files — auto-generated, not needed
__pycache__/
*.pyc

# Jupyter notebook checkpoints — auto-generated
.ipynb_checkpoints/